# Public URL of the hosted Trunc MCP server. The /mcp page uses this to
# build the hosted client config that users copy-paste into Claude Desktop.
# Override this if you self-host the MCP server on your own domain.
VITE_HOSTED_MCP_URL=https://trunc-mcp.onrender.com/mcp

# Base URL of the Express backend
SHORTENER_API_BASE=http://localhost:5050

# HTTP listen port (ignored by stdio transport)
PORT=3001

# Required for stdio: paste the JWT issued by the web app here.
# Leave empty if you're only running the HTTP transport and passing
# the token per-request via the Authorization header.
TRUNC_MCP_TOKEN=
